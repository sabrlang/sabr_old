$++to macro dup call 1 + swap to end
$--to macro dup call 1 - swap to end
$--store macro dup fetch 1 - swap store end

$dcr macro dup call rot end

$+to macro dcr + swap to end
$-to macro dcr - swap to end
$*to macro dcr * swap to end
$/to macro dcr / swap to end

$<<to macro dcr << swap to end
$>>to macro dcr >> swap to end

$true macro -1 end
$false macro 0 end
$cr macro '\n' putc end
$puts macro loop dup 0 > while swap putc 1- end drop end
$putsln macro puts cr end

$at macro swap 8 * + end